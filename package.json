{
  "name": "wmv-file-splitter",
  "version": "1.0.0",
  "description": "",
  "main": "WMVSplitter.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc -w --skipLibCheck",
    "dev": "nodemon --exec ts-node -- src/WMVSplitter.ts",
    "build": "tsc && cp package.json dist/package.json"
  },
  "dependencies": {
    "nodemon": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^16.18.47",
    "typescript": "^4.4.3"
  }
}
